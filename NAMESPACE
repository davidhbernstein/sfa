export(zsfm, lcsfm, selsfm, ivsfm, copsfm, psfm, sfm, ttsfm, npsfm, sfa_diagnostics, efficiency_ci, skewness_test, marginal_effects, efficiency, meanefficiency, simulation_se, TIC, vuong, lcsfm_homogeneity, spec_test, spec_test_all, sfma, pcomposed, dcomposed, data_gen_p, data_gen_cs, summary.sfareg, print.sfareg, psfm_bootstrap)

import("methods")
import("plm") # import whole package
import("minqa") # import whole package
import("randtoolbox") # import whole package
import("mnormt") # import whole package
import("truncnorm") # import whole package
import("tmvtnorm") # import whole package
import("Formula") # import whole package
import("pso") # import whole package 
import("numDeriv") # import whole package
import("gsl") # import whole package
import("pbapply") # import whole package
import("parallel") # import whole package

importFrom("utils", "head")
importFrom("stats", "coef",         "dnorm",          "dt",      "integrate", "lm",
                    "model.matrix", "model.response", "optim",   "pnorm",     "pt",
                    "qnorm",        "rcauchy",        "rexp",    "rnorm",     "rt",
                    "qt",
                    "as.formula",   "complete.cases", "formula", "sd",        "runif",
                    "ave",          "cor",            "terms",   "lm.fit",
                    "plogis",       "rbinom",         "rgamma",  "rlnorm",    "rweibull",
                    "dlnorm",        "dweibull",      "qlnorm",    "qweibull",
                    "fitted",        "residuals",     "optimize")
S3method(summary, sfareg)
S3method(print, sfareg)
S3method(coef, sfareg)
S3method(vcov, sfareg)
S3method(logLik, sfareg)
S3method(nobs, sfareg)
S3method(predict, sfareg)
S3method(fitted, sfareg)
S3method(residuals, sfareg)
S3method(print, npsfareg)
S3method(summary, npsfareg)
S3method(fitted, npsfareg)
S3method(residuals, npsfareg)
S3method(nobs, npsfareg)
S3method(plot, sfareg)
S3method(print, sfadiag)
S3method(print, sfa_vuong)
S3method(print, sfa_mlrt)
S3method(print, sfa_spec_test)
S3method(print, sfa_spec_grid)
S3method(print, sfma)

## Delayed registration: `sandwich` is in Suggests, not Imports, so the
## generics do not exist at load time. The `pkg::generic` form registers these
## only once sandwich is loaded, which is what makes dispatch work in an
## INSTALLED package. Without it the methods are defined but unreachable --
## sandwich::estfun() on an "sfareg" fit failed with "no applicable method"
## while devtools::load_all() masked the problem by exporting everything.
S3method(sandwich::bread, sfareg)
S3method(sandwich::estfun, sfareg)
S3method(texreg::extract, sfareg)
